{
  "name": "standup-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "test:watch": "nodemon -x \"node --test\" -w src -w test",
    "bdd": "cucumber-js --require features/steps/**/*.js",
    "bdd:report": "cucumber-js --require features/steps/**/*.js --format html:reports/cucumber.html --format json:reports/cucumber.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "script": {
    "test": "node --test"
  },
  "dependencies": {
    "fastify": "^5.6.0"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^12.2.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4",
    "vitest": "^3.2.4"
  },
  "type": "module"
}
